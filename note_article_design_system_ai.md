# デザインシステムって、AIに何を渡せばいいんだろう

AIを使って画面を作っていると、なんとなく「伝わってない感」がある。こっちはButtonコンポーネントを渡しているのに、なぜかそれっぽいけど微妙に違うものが出てくる。たぶん、見た目は渡せているけど「なぜそのデザインなのか」が渡せていない。それってつまり、デザインシステムそのものの作り方を変えないといけないのかもしれないと思って、ここ数週間いろいろ調べていた。

---

## デザインシステムは「配給」だった

これまでのデザインシステム（DS）の役割は、ひとことで言うと「正しい部品を正しく渡す」配給だったと思う。

FigmaのコンポーネントライブラリをメンテしてStorybookにカタログを作って、「このボタンはここで使う、このカラーはブランドカラー、このスペーシングで揃える」という形で、チーム全員が同じ部品を使えるようにする。視覚的に揃っていることが一貫性の証明だった。

それ自体は間違っていないし、今でも有効だと思う。でも、AIがチームに加わってくると、このモデルの限界が見えてきた。

---

## でも、AIには「判断の理由」が要る

David Hoangが2026年4月に書いた記事「Design Systems are now Inference Systems」が、この変化をうまく言語化している。

変化の軸を3つ挙げていて、

- 静的なパターン集 → AIが文脈に応じて変形できる適応的パラメータへ
- 人間向けドキュメント → 機械が読めるコンテキストへ
- 逸脱をNGとして弾く → 逸脱をデータとして学習するフィードバックループへ

という整理。なかでもこの一文が刺さった。

> "Deviations stop being 'don'ts' and start being data. The system stops being a wall and starts being a sensor."

逸脱を「やっちゃダメ」で終わらせるんじゃなくて、「なぜここでDSから外れたのか」を観察して、システム自体を育てていく。壁からセンサーへ。なるほどなあ、と思った。

参照: https://www.proofofconcept.pub/p/design-systems-are-now-inference

---

## 「意図の地図」ってどういうことか

じゃあ具体的に何をすればいいのか、というところで参考になった例が3つある。

**shadcn/ui のvariant命名**

shadcn/uiのButtonコンポーネントには `variant="destructive"` とか `variant="ghost"` という命名がある。ただの見た目の指定じゃなくて、「これは取り消せない破壊的な操作か」「これは背景に溶け込むべき控えめなアクションか」という意図が名前に乗っている。

さらにshadcn/uiはnpmパッケージとして配布するんじゃなくて、コードをそのまま自分のリポジトリにコピーする設計になっている。公式が「Open Code for LLMs to read, understand, and improve」と言っているように、AIがコードを直接読める状態にすること自体がAI時代の設計思想だと思う。

参照: https://ui.shadcn.com/

**Figma Variables の意味語命名**

Figma VariablesでColorトークンを `color/blue/500` じゃなくて `color/feedback/destructive` と命名すると、AIがデザインファイルを読んだとき「この色は警告や破壊的操作に使うものだ」という文脈を読み取れる。`#0066FF` という値を渡すんじゃなくて `most-noticeable-action` という意図を渡す、という発想の転換。

**Decision DNA（Hasegawa氏）**

Yasuhisa Hasegawaさんが提唱している「Decision DNA」という概念がある。AIに「ここが正解」というルールを渡すんじゃなくて、判断に必要な4つの視点（文脈・認知・較正・明文化）をインストールするフレームワーク。

これはまだ実装例がある段階ではなくて、概念として提唱されている途中。でも「AIにルールではなく視点を渡す」という方向性は、自分のなかで一番しっくりきている。

参照: https://yasuhisa.com/could/article/decision-dna/

---

## 2つの考え方があるらしい

この議論、英語圏でも立場が割れていておもしろい。

|  | アプローチ |
|---|---|
| Brad Frost | 高品質な部品でAIの動きを縛る |
| Hasegawa / David Hoang | 意図を渡してAIに推論させる |

Brad Frostはこう言っている。

> "This is what distinguishes DS+AI from vibe coding; the AI is deliberately constrained to using the high-quality design system materials."

AIをvibe codingから区別するのは、高品質なDS素材だけを使うように「意図的に制約している」ことだ、という立場。テキストエリアが必要な画面でテキストインプットしか登録されていなければ、アクセシブルでないUIが生成されてしまう。だから部品のカバレッジと品質でAIを縛る。

一方でHasegawa氏やHoangは、部品の前に「なぜその部品がそこにあるか」の意図を埋め込むべきだと言っている。制約じゃなくて推論の材料を渡す。

どちらが正解かはたぶんチームの状況やプロダクトの複雑さによる、というのが正直なところで、自分もまだよくわかっていない。

参照: https://bradfrost.com/blog/post/agentic-design-systems-in-2026/

---

## 今すぐできることは、たぶん小さい

現時点でどこまで実現されているかを整理するとこんな感じ。

- **すでにある**: 意味語トークン（Figma Variables / shadcn）、Storybook MCP（AIがStorybookを直接参照できる接続口）
- **実験中**: Decision DNA的なメタデータ、AIが読む前提でのドキュメント設計
- **まだない**: 「文脈からふさわしい判断を推論して説明できるDS」の完成形

一番手近な入り口は、shadcn/uiのvariant命名を真似て自チームのコンポーネントに意味語を付け始めることだと思う。あとはStorybookのStoryに「このコンポーネントはどういう判断のときに使うか」を一行書き添えるだけでも、AIとの協業の質が変わってくるんじゃないかと思っている。

---

デザインシステムに「なぜ」を書き始めることが、たぶん今一番手近な入り口なんだと思う。完成形がどこにあるかはまだよくわからないけど、「AIが読む前提でドキュメントを書く」という視点は持ち続けておきたいなと思った。
